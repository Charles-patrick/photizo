import type { Metadata } from "next";
import Support from "@/components/sections/realtor/Support";

export const metadata: Metadata = { title: "Support | Photizo Realtor" };

export default function Page() {
  return <Support />;
}
