import type { Metadata } from "next";
import Support from "@/components/sections/dashboard/Support";

export const metadata: Metadata = { title: "Support | Photizo Dashboard" };

export default function Page() {
  return <Support />;
}
